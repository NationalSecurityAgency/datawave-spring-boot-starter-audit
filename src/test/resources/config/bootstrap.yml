spring:
  cloud:
    bus:
      enabled: false
    consul:
      enabled: false
    config:
      enabled: false
      discovery:
        enabled: false
    stream:
      default-binder: integration
  main:
    banner-mode: "OFF"

